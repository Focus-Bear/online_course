export enum USER_ROLES {
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum COURSE_ORDER {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum LessonCompletionStatus {
  SELF_TAUGHT = 'SELF_TAUGHT',
  TUTORIAL = 'TUTORIAL',
}

export enum Language {
  EN = 'en',
  ES = 'es',
}

export enum UserTypes {
  STANDARD = 'STANDARD',
  ADMIN = 'ADMIN',
}
